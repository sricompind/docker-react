language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sricompind/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sricompind/docker-react npm run test -- --coverage